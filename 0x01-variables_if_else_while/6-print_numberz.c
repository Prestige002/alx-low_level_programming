#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - print all single digit number of base 10
 * starting from 0
 * followed by new line
 *
 * Return: zero
 */
int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
	{
		putchar("%i", i);
	}
	putchar("\n");
	return (0);
}
